# Texture Studio MC — Frontend

Ver `../docs/README.md` (instalación/desarrollo local) y `../docs/COMPONENTES.md` (estructura de componentes) en la raíz del repo.

Scaffold: Vite + React + TypeScript + `@react-three/fiber`/`drei`.
