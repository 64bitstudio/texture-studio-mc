# 090 — Editor de animación: timeline de keyframes y presets paramétricos

## Objetivo
Construir el editor de animación (Etapa 4): timeline libre de keyframes (agregar/mover/eliminar, reproducir/pausar/scrub) que respeta la jerarquía de huesos, más presets paramétricos para los 5 nombres que FreeMinecraftModels reconoce automáticamente (`spawn`/`idle`/`walk`/`attack`/`death`). Nace de `docs/definiciones/modelado-3d-custom-y-generacion-con-ia.md` ("Impacto estimado" ticket 11; HU-9, HU-10). Depende de los tickets 084 (jerarquía) y 086 (confirmar modelo).

## Criterios de aceptación (TDD)
- Dado el editor de animación abierto, cuando elijo un preset y ajusto sus parámetros (velocidad, amplitud), se genera un set de keyframes reproducible de inmediato en el visor 3D, guardado con ese nombre exacto (HU-9).
- Dado un modelo cuya jerarquía no coincide con lo que un preset espera, cuando intento aplicarlo, veo un aviso claro de qué huesos faltan, sin romper el modelo (HU-9).
- Dado que creo una animación `walk` sin una `idle` en el mismo mob, veo una advertencia explícita (restricción documentada del plugin), sin bloquear la creación (HU-9).
- Dado un nombre fuera de los 5 reconocidos, la app lo guarda igual, indicando que solo será disparable manualmente por código del servidor (HU-9).
- Dado que agrego un keyframe en un momento específico para un hueso, aparece en el timeline y el visor 3D interpola entre keyframes al reproducir (HU-10).
- Dado que reproduzco la animación, un hueso padre con rotación animada mueve visualmente a sus huesos hijos en el preview (HU-10).

## Hecho

Implementado en la rama `feature/090-editor-animacion-timeline-y-presets`.

- **`frontend/src/projectStorage.ts`**: `AnimationKeyframe` gana `position`/`scale` opcionales (además de `rotation`), aditivo. `updateMobGeometry` acepta también `animations`.
- **`frontend/src/geometry/hierarchy.ts`**: **hallazgo real** (ver detalle abajo) -- `applyDefaultHierarchy` ahora excluye cajas con `pivot` (hoy, solo las 8 patas de la Araña) de la jerarquía automática. Nueva `export function hasAnyHierarchy` (antes privada en `ModelEditor3D.tsx`).
- **`frontend/src/components/Viewer3D.tsx`**: nuevo renderizado jerárquico (`HierarchyPartGroup`, `<group>` anidado por `parentId`, mismo patrón que `ModelEditor3D.tsx`) + nuevo prop opcional `boneOverrides` (poses animadas DELTA que se suman/multiplican sobre la pose base). Cero cambio de comportamiento para cualquier geometría sin jerarquía (los 4 mobs vainilla tal como los sirve el backend hoy).
- **`frontend/src/animation/interpolation.ts`** (nuevo, puro): `sampleBoneAtTime`/`sampleAnimationAtTime`/`resolveLoopedTime` -- interpolación lineal, loop/clamp de tiempo.
- **`frontend/src/animation/presets.ts`** (nuevo, puro): `PRESET_ROLE_BONES` (tabla por mobId, análoga a `DEFAULT_HIERARCHY`) + `generatePresetAnimation`/`getMissingBonesForPreset` para los 5 presets (`spawn`/`idle`/`walk`/`attack`/`death`), con validación real contra la geometría actual (no solo la tabla estática).
- **`frontend/src/animation/timelineEditing.ts`** (nuevo, puro): CRUD manual de keyframes (`addKeyframe`/`moveKeyframe`/`removeKeyframe`/`updateKeyframeValue`) + `RECOGNIZED_ANIMATION_NAMES`/`isRecognizedAnimationName` + `checkWalkRequiresIdle`.
- **`frontend/src/components/AnimationEditor.tsx`** (nuevo): editor completo -- lista de animaciones, generación desde preset con sliders de velocidad/amplitud, creación de animación vacía, timeline manual (selector de hueso + keyframes editables + agregar en el tiempo actual del scrubber), reproducción con play/pause/scrub sobre un `Viewer3D` animado.
- **`frontend/src/components/MobEntryCard.tsx`**/**`Proyecto.tsx`**: nuevo ítem "Editar animaciones" en el menú "⋮", siempre habilitado.
- **`frontend/src/App.tsx`**: nueva vista `'editor-animacion'`, reusando `effectiveAssetState` (sin fetch propio).

**Hallazgo real #1 (bug pre-existente, no introducido por este ticket)**: investigando cómo animar la jerarquía en `Viewer3D.tsx`, se descubrió que `applyDefaultHierarchy` (ticket 084) convertía la `position` de CUALQUIER caja a relativa-al-padre, incluyendo cajas con `pivot` (patas de la Araña) -- pero para esas cajas `position` es un punto de referencia PRE-rotación, no la posición final, así que la conversión la corrompía. Nadie lo había notado porque el QA en vivo del ticket 084 solo probó el Creeper (sin `pivot`). Fix acotado: `applyDefaultHierarchy` excluye cajas con `pivot` de la jerarquía automática -- quedan sin `parentId`, cero cambio de comportamiento para ellas (siguen renderizando exactamente como antes, vía el camino `pivot` flat de `Viewer3D.tsx`).

**Decisión de alcance señalada explícitamente**: las cajas con `pivot` (patas de la Araña) quedan FUERA del anidado padre-hijo del visor -- se pueden animar en su lugar (rotar alrededor de su propio pivote, verificado en vivo con el preset "walk" de la Araña), pero NO las arrastra un padre animado (el thorax). Reconciliar completamente `pivot` con la jerarquía `parentId` (para que también las patas hereden el movimiento de un thorax animado) queda fuera de este ticket -- es un caso de uso mucho más raro (las 8 patas ya se animan bien individualmente para un ciclo de marcha) y el fix habría requerido tocar código de cámara/encuadre ya afinado y probado (`geometryBounds.ts`, ticket 077) con más riesgo del que este ticket justificaba.

**Hallazgo real #2 y #3 (UX, corregidos en el camino, ver detalle en `docs/COMPONENTES.md`)**: valores senoidales casi-cero mostrados en notación exponencial fea (fix: redondeo a 3 decimales) y `currentTime` creciendo sin límite durante reproducción en loop (fix: envolver con `resolveLoopedTime` en cada tick, no solo al muestrear).

**Desviación de alcance menor**: HU-10 pide editar keyframes "de posición/rotación/escala" -- el timeline manual implementado edita rotación (X/Y/Z) via inputs numéricos; `position`/`scale` son channels soportados por el modelo de datos/interpolación/CRUD (`addKeyframe`/`updateKeyframeValue` aceptan cualquiera de los 3), y los usa el preset "spawn" (`position`), pero la UI del timeline no expone todavía inputs para editarlos a mano (solo rotación). Señalado explícitamente -- ampliar la UI a los 3 canales es un agregado incremental de bajo riesgo sobre este mismo código, no un rediseño.

**Tests**: `test/interpolation.spec.ts` (nuevo, 8 casos), `test/presets.spec.ts` (nuevo, 14 casos), `test/timelineEditing.spec.ts` (nuevo, 12 casos), `test/hierarchy.spec.ts` (+2 casos: el hallazgo del `pivot` y `hasAnyHierarchy`). Suite completa: **346/346 tests en verde**. `tsc -b`, `oxlint`, `npm run build` sin hallazgos.

**Revisión visual en vivo** (Claude in Chrome, proyecto con Zombie y Araña reales): preset "idle" generado y reproducido -- cabeza oscilando visiblemente; keyframe manual agregado en el tiempo del scrubber (continuidad preservada, arranca del valor interpolado, no de 0) y luego eliminado; edición manual de un valor de rotación confirmada; preset "walk" generado -- advertencia "walk sin idle" apareció/desapareció correctamente al quitar/agregar "idle"; **Guardar animaciones + recarga completa de la página confirmó persistencia real** de ambas animaciones. Sobre la Araña: geometría renderiza en su pose de reposo normal (sin corrupción, confirma el fix del hallazgo #1), preset "walk" con las 8 patas moviéndose de forma coherente. **Verificación específica y concluyente del último criterio de aceptación** (hueso padre arrastra a los hijos): se agregó a mano un keyframe en el hueso raíz `body` del Zombie con una rotación Y de 60° -- el modelo COMPLETO (torso, cabeza, ambos brazos, ambas piernas) rotó junto en el visor, confirmando que el anidado padre-hijo de `Viewer3D.tsx` funciona correctamente. Sin errores en consola en ningún momento del QA.
