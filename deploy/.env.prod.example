# Ticket 001 -- plantilla de deploy/.env.prod (real, vive SOLO en la VM
# en /home/ubuntu/secrets/texture-studio-mc/.env.prod, gitignored). Ver
# deploy/.env.dev.example para el porqué de `skipVaultSecrets: true` y
# el cambio señalado en el ticket 087 (GEMINI_API_KEY, pendiente de
# migrar a Vault -- trabajo de DevOps, no de este ticket).

# Imagen a desplegar -- la asigna corePipeline (SHA del commit), no a mano.
IMAGE_TAG=

# Ticket 087 -- ver deploy/.env.dev.example para el detalle completo.
GEMINI_API_KEY=
GEMINI_MODEL=
