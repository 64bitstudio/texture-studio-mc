# Ticket 001 -- plantilla de deploy/.env.qa (real, vive SOLO en la VM en
# /home/ubuntu/secrets/texture-studio-mc/.env.qa, gitignored). Ver
# deploy/.env.dev.example para el porqué de `skipVaultSecrets: true`
# (este proyecto no tiene base de datos ni secretos propios).

# Imagen a desplegar -- la asigna corePipeline (SHA del commit), no a mano.
IMAGE_TAG=
